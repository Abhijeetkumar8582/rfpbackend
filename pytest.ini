[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# If pytest fails loading plugins (e.g. opik), run: set PYTEST_DISABLE_PLUGIN_AUTOLOAD=1
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
