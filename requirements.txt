# RFP Backend — Python 3.11+
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.25
alembic>=1.13.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pydantic[email]>=2.5.0
python-dotenv>=1.0.0
email-validator>=2.1.0
python-multipart>=0.0.6

# DB driver (choose one)
# MySQL (RDS)
PyMySQL>=1.1.0
# PostgreSQL
# psycopg2-binary>=2.9.9
# SQLite for local dev (stdlib, no extra package)

# Auth / security
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0

# HTTP client (for scripts/tests)
httpx>=0.25.0

# File processing + AI
openai>=1.0.0
boto3>=1.34.0
pypdf>=4.0.0
openpyxl>=3.1.0
chromadb>=0.4.0

# Reranking (cross-encoder for agentic search)
sentence-transformers>=2.2.0

# PDF image detection + OCR (scanned PDFs, image-heavy documents)
pymupdf>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0

# Email (SendGrid)
sendgrid>=6.11.0

# Sortable unique IDs for conversation_id (conv_<ULID>)
python-ulid>=2.0.0
