# Copy to .env and fill in values
# Database — RDS MySQL (ensure host is reachable: security group, VPN). For local-only: DATABASE_URL=sqlite:///./rfp.db
DATABASE_URL=mysql+pymysql://researchagent:Admin1234@database-2.cfmeekcmyemg.ap-south-1.rds.amazonaws.com:3306/RFP

# App
APP_ENV=development
SECRET_KEY=change-me-in-production
API_V1_PREFIX=/api/v1

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Document upload: embedding + GPT category + S3
OPENAI_API_KEY=sk-...
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_CHAT_MODEL=gpt-4o-mini

# S3 (file storage — files stored at project_id/cluster/filename)
S3_BUCKET=your-bucket-name
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
